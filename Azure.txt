apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: microservice-1-dev
  namespace: argocd   # ArgoCD control-plane namespace
spec:
  project: default

  # --- Where is your GitOps repo ---
  source:
    repoURL: 'https://github.com/your-org/your-repo.git'  # change
    targetRevision: main                                  # branch name
    path: gitops/charts/app-chart                         # Helm chart path
    helm:
      valueFiles:
        - ../../microservices/microservice-1/dev/values.yaml

  # --- Where to deploy in cluster ---
  destination:
    server: https://kubernetes.default.svc  # in-cluster k8s API
    namespace: microservice-1-dev           # namespace for MS1

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
