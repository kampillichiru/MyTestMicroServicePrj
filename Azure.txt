- name: Register GitHub Repo in ArgoCD
  run: |
    argocd repo add https://github.com/${{ github.repository }}.git \
      --username ${{ secrets.GH_USER }} \
      --password ${{ secrets.GH_PAT }}
